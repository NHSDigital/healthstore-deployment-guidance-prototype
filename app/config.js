// Use this file to change prototype configuration.

module.exports = {
  // Service name
  serviceName: 'Healthstore deployment guidance: a HTML prototype',

  // Port to run the prototype on locally
  port: 3000
}
